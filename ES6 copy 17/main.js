// Only change code below this line

// Only change code above this line
class Vegetable {
    constructor(name) {
        this.name = name;
    }
}
const carrot = new Vegetable('carrot');
console.log(carrot.name); // Should display 'carrot'

// Используйте ключевое слово class и напишите конструктор для создания класса Vegetable.Класс Vegetable позволяет создать объект vegetable с именем свойства, которое передается конструктору.x