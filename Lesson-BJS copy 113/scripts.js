function checkSign(num) {
    return num > 0 ? "positive"
        : num < 0 ? "negative"
            : "zero";
}

checkSign(10);
//В функции checkSign используйте несколько условных операторов - следуя рекомендуемому формату, используемому в findGreaterOrEqual - чтобы проверить, является ли число положительным, отрицательным или нулевым. Функция должна возвращать положительный, отрицательный или нулевой.