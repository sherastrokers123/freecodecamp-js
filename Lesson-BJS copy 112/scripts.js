function checkEqual(a, b) {
    return a === b ? "Equal" : "Not Equal";
}

checkEqual(1, 2);
// Используйте условный оператор в функции checkEqual, чтобы проверить, равны ли два числа или нет. Функция должна возвращать либо строку Equal, либо строку Not Equal.